export class Student {
    id: string;
    name: string;
    firstName: string;

    constructor(id:string, name:string, firstName:string){
        this.id = id;
        this.name = name;
        this.firstName = firstName;
    }
  }